name: Sync dev branch

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Force update dev to main
        run: git push origin HEAD:refs/heads/dev --force
