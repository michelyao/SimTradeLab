# 任务列表：George 策略状态追踪实现

## 任务分解

### 1. 初始化全局状态变量
- [ ] 在 `set_params()` 函数中初始化 `g.stock_states` 字典
- [ ] 初始化 `g.limit1` 列表（状态为 1 的股票）
- [ ] 初始化 `g.limit2` 列表（状态为 2 的股票）

### 2. 修改 `interval_handle` 函数
- [ ] 在循环中获取当前状态
- [ ] 从 `g.stock_states` 中获取上一次状态
- [ ] 更新 `g.stock_states` 中的状态信息
- [ ] 根据状态值更新 `g.limit1` 和 `g.limit2` 列表

### 3. 实现状态转移检测
- [ ] 在 `_proc_hit_board` 函数中检查状态转移条件
- [ ] 当 `last_status in [0, -1, -2] and current_status == 2` 时记录买入日志
- [ ] 否则记录打板未达到条件的日志

### 4. 测试验证
- [ ] 验证状态记录正确性
- [ ] 验证状态转移检测逻辑
- [ ] 验证日志输出格式

## 依赖关系
- 任务 1 是基础，必须先完成
- 任务 2 依赖任务 1
- 任务 3 依赖任务 2
- 任务 4 依赖任务 3
