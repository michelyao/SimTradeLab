# 任务列表：refactor-hitboard-single

## 任务分解

### 1. 验证当前实现
- [x] 阅读 `hit_top_single.py` 中的 `_proc_hit_board` 函数
- [x] 理解当前的 `get_snapshot` 调用方式
- [x] 确认数据缓存情况

### 2. 按规范优化 `_proc_hit_board` 函数
- [x] 确保每次调用 `get_snapshot` 都从接口获取最新数据
- [x] 验证返回的快照数据不为空
- [x] 验证必要字段存在（`up_px`、`last_px`、`offer_grp`）
- [x] 验证 `offer_grp` 包含第 5 档数据
- [x] 正确提取 `offer_grp[5][0]`（第 5 档价格）和 `offer_grp[5][1]`（第 5 档委托量）

### 3. 保持业务逻辑
- [x] 保持打板检测条件：`level_5_price == up_px and level_5_order <= 5000`
- [x] 保持现有的日志输出格式
- [x] 保持异常处理逻辑

### 4. 代码风格检查
- [x] 确保 Python 3.5 兼容性
- [x] 不使用 f-string，使用 `.format()` 或 `%` 格式化
- [x] 手动添加行号到日志
- [x] 遵循防御式编程风格

### 5. 测试验证
- [x] 验证函数能正确处理有效的快照数据
- [x] 验证函数能正确处理空快照数据
- [x] 验证函数能正确处理缺失字段的情况
- [x] 验证函数能正确处理不完整的 `offer_grp` 数据

## 依赖关系
- 任务 1 是前置条件
- 任务 2-4 可以并行进行
- 任务 5 依赖于任务 2-4 完成

## 实现总结

### 修改的文件
- `strategies/hit_top_single.py` - 优化了 `_proc_hit_board` 函数（第 131-186 行）

### 关键改进
1. **数据验证层次**：
   - 快照数据不为空检查
   - 股票代码存在性检查
   - 必要字段（up_px、last_px）存在性检查
   - offer_grp 数据结构有效性检查
   - 第 5 档数据完整性检查

2. **接口规范遵循**：
   - 正确处理 get_snapshot 返回的 dict 结构
   - 使用 .get() 方法安全访问嵌套数据
   - 验证 offer_grp[5] 存在且包含至少 2 个元素

3. **业务逻辑保持**：
   - 打板检测条件不变：`level_5_price == up_px and level_5_order <= 5000`
   - 日志输出格式保持一致
   - 异常处理逻辑完整

4. **代码风格合规**：
   - Python 3.5 兼容（无 f-string）
   - 防御式编程风格
   - 手动行号跟踪
